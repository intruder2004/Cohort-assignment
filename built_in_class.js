//Built in date class
const now = new Date(); // Current date and time
console.log(now.toISOString()); // Outputs the date in ISO format

//Built in map function
const mappy = new Map();
mappy.set("name", "Neschal");
mappy.set("age", 20);

console.log(mappy.get("name"));
